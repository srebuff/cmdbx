
### asm 'asm/types.h' file not found

```
apt-get install -y linux-libc-dev gcc-multilib
```

### "llvm-strip": executable file not found in $PATH

```
apt-get install -y llvm
```

### counter.c:5:10: fatal error: 'bpf/bpf_helpers.h' file not found

```
apt-get install -y libbpf-dev
```